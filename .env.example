# ABHA gateway configuration
# Set ABHA_API_URL to the base URL of the ABHA gateway you are using. Example:
# ABHA_API_URL=https://abha-gateway.example.gov

# Option 1: Provide a static bearer token (useful for testing)
# ABHA_API_TOKEN=eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9...

# Option 2: Provide OAuth client credentials and token endpoint
# ABHA_TOKEN_URL=https://auth.example.gov/oauth2/token
# ABHA_CLIENT_ID=your-client-id
# ABHA_CLIENT_SECRET=your-client-secret

# NOTE: Adjust the paths/endpoint in `lib/abha.ts` if the provider uses a different
# search path than `/patients/search`.
